import * as React from 'react';

export interface ProductDetailProps {
}

export interface ProductDetailState {
}

export default class ProductDetail extends React.Component<ProductDetailProps, ProductDetailState> {
  constructor(props: ProductDetailProps) {
    super(props);

    this.state = {
    }
  }

  public render() {
    return (
      <div>
        as
      </div>
    );
  }
}
